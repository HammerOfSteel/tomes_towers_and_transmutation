# TT&T — TODO Overview

> Master index. All detailed tasks live in subfolders. Work top-to-bottom — each section depends on the ones before it.
> **Legend:** ✅ Done · 🔲 To Do · 🚧 In Progress · ⚠️ GAP (not yet tracked)

---

## Recommended Work Order

```
01 Overworld Studio        → design tools that generate world data
02 Game World Integration  → use that data to build the 3D playable world  ← DEPENDS ON 01
03 Procedural Pipeline     → builders & creator ecosystem  ← FEEDS 02 + UI tools
04 Characters              → species, enemies, NPCs  ← DEPENDS ON 03
05 Content                 → quests, story, lore  ← DEPENDS ON 04
06 Game Systems            → abilities, talents, tomes, alchemy, companions, defense, audio, saves  ← DEPENDS ON 04+05
07 Testing                 → lock down everything above
08 Polish & Release        → performance, UX, fundraising  ← LAST
```

> ⚠️ **Correction (2026-08 audit, see G15):** "05 depends on 04, 06 depends
> on 04+05" is not literally true — 05's quest/story content already
> references reward and event types (follower recruits, transmutation
> items, defense escalation) that only 06's systems can deliver, and those
> 06 systems are currently zero-code, design-only. In practice: **finalize
> data models/schemas for 06's systems (TRS, ATC, FCS, TDE, Save) before
> locking down 05's content**, so quest/story authoring doesn't reference
> reward types that don't exist yet. Full 06 implementation can still trail
> 05 — only the schemas need to come first.

---

## Section Index

### [01 — Overworld Studio](./01-overworld-studio/README.md)

Design tools (Overworld Studio web app) that produce world data consumed by the game.

| File                                                                                     | Topic                                                                     | Status |
| ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------ |
| [OW-D Dwelling Layer](./01-overworld-studio/OW-D-dwelling-layer.md)                      | Floor plan renderer for houses                                            | ✅     |
| [OW-E Layer Navigation](./01-overworld-studio/OW-E-layer-navigation.md)                  | Click-through realm→city→dungeon                                          | ✅     |
| [OW-F4 Drill-Down Chain](./01-overworld-studio/OW-F4-drill-down-chain.md)                | Solar→Planet→Realm→City full chain                                        | ✅     |
| [Asset Library](./01-overworld-studio/asset-library.md)                                  | Browse & manage all generated assets                                      | 🚧     |
| [Game Inventory](./01-overworld-studio/game-inventory.md)                                | Master contract for procedural asset outputs needed from Overworld Studio | 🚧     |
| [Procedural Asset Designer](./01-overworld-studio/procedural-designer/asset-designer.md) | Visual designer for DNA-based entities                                    | ✅     |
| [Procedural Tile Designer](./01-overworld-studio/procedural-designer/tile-designer.md)   | Tile variant tool for biome/dungeon tiles                                 | ✅     |

**Shipped (OW-A thru OW-F4):** Realm Map, Dungeon tab, Cave tab, 3D Planet, Hex Planet, Planet DNA, Solar System, Dwelling Layer (building-viewer), Layer Navigation (breadcrumbs + drill-downs), Full Solar→Planet→Realm→City chain ✅

---

### [02 — Game World Integration](./02-game-world-integration/README.md)

Turn Overworld Studio output into the actual 3D playable world. **Depends on 01.**

| File                                                                            | Topic                                     | Status |
| ------------------------------------------------------------------------------- | ----------------------------------------- | ------ |
| [Realm Integration](./02-game-world-integration/realm-integration.md)           | Generate 3D terrain from realm map biomes | 🚧     |
| [Settlement Integration](./02-game-world-integration/settlement-integration.md) | Place generated settlements in 3D world (live pipeline already builds/roads/NPCs; boundary toast added, LOD still open; building placement is now footprint-aware and interiors route through `sceneManager.loadDungeon()` with real multi-floor staircases) | 🚧     |
| [Dungeon Integration](./02-game-world-integration/dungeon-integration.md)       | Dungeon entrances/exits on realm map (live enter/exit works; exit-position bug fixed; site-family metadata now live + shown in toast; quest/reward consumption still open) | 🚧     |
| [Cave Integration](./02-game-world-integration/cave-glade-integration.md)       | Cave & glade entrances on realm map — live wiring + minimap + persistence done, floor transition deferred | 🚧 In Progress |

> 🚧 **Realm Integration's core data-transform slices (RI-1 through RI-4) have shipped** — `src/world/RealmToTerrain.ts` maps realm cells to `TileDNA` placements with height smoothing and biome-transition flags, `src/world/RealmRiverMesh.ts` builds width-varying river ribbons, and `src/world/ChunkManager.ts` provides generic chunk load/unload streaming logic. All pure and unit-tested (34 new tests); wiring these into an actual `OverworldScene.ts` terrain renderer, plus settlement/dungeon/cave integration, are still open.

---

### [03 — Procedural Pipeline](./03-procedural-pipeline/README.md)

Every builder (NPC, Enemy, Building, Prop) serves two consumers: game runtime + designer UI.

| File                                                                           | Topic                                           | Status |
| ------------------------------------------------------------------------------ | ----------------------------------------------- | ------ |
| [PROC-A Entity Registry](./03-procedural-pipeline/PROC-A-entity-registry.md)   | Central registry + base DNA types               | ✅     |
| [PROC-B Creator Tools](./03-procedural-pipeline/PROC-B-creator-tools.md)       | NPC/Enemy/Building/Prop atelier tools           | 🚧     |
| [PROC-C World Generation](./03-procedural-pipeline/PROC-C-world-generation.md) | Seeded placement plan for all entities          | 🔲     |
| [PROC-D Creative Mode](./03-procedural-pipeline/PROC-D-creative-mode.md)       | DevLab integration + custom blueprint overrides | 🔲     |
| [PROC-E Asset Retirement](./03-procedural-pipeline/PROC-E-asset-retirement.md) | Remove all GLB/external asset load paths        | 🔲     |
| [Environment Art System](./03-procedural-pipeline/environment-art-system.md)   | Phase 5: code-first vs Kenney toggle            | 🔲     |

---

### [04 — Characters](./04-characters/README.md)

Species, princess creator, enemies, NPCs. **Depends on 03 (builders).**

| File                                                              | Topic                                               | Status |
| ----------------------------------------------------------------- | --------------------------------------------------- | ------ |
| [Princess Creator](./04-characters/princess-creator.md)           | PC phases: defaults, library, game integration      | 🚧     |
| [New Species Expansion](./04-characters/new-species-expansion.md) | NS phases: Elf, Celestial, Draconic + all 21 mapped | 🚧     |
| [Enemy System](./04-characters/enemy-system.md)                   | B phases: roster, AI, encounter design              | 🚧     |

---

### [05 — Content](./05-content/README.md)

Quests, story arcs, lore. **Depends on 04 (characters must exist first).**

| File                                                   | Topic                                              | Status |
| ------------------------------------------------------ | -------------------------------------------------- | ------ |
| [Quests](./05-content/quests.md)                       | Phase C: 5×4 species quests + 5 general            | 🔲     |
| [Story Arcs](./05-content/story-arcs.md)               | Phase E1: Act I arcs all 4 species                 | 🚧     |
| [Solmor Encounters](./05-content/solmor-encounters.md) | Phase E2 + NS5: 3-stage dialogue for all 7 species | 🚧     |
| [Lore Books](./05-content/lore-books.md)               | All placed lore books across floors + overworld    | 🚧     |

---

### [06 — Game Systems](./06-game-systems/README.md)

Abilities, talents, tomes/research, alchemy/transmutation, companions, tower defense, audio, saves. **Depends on 04 + 05** for full gameplay integration.

| File                                                                                     | Topic                                                                            | Status |
| ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ------ |
| [Abilities & Talents](./06-game-systems/abilities-talents.md)                            | Phase D: full ability sets + talent trees                                        | 🚧     |
| [Tomes, Research & Spellcraft](./06-game-systems/tomes-research-spellcraft.md)           | Book taxonomy, personal library, school mastery, research bridge, spell mutation | ⚠️ GAP |
| [Alchemy, Transmutation & Crafting](./06-game-systems/alchemy-transmutation-crafting.md) | Reagents, refinement tiers, formulas, facility-gated crafting, instability       | ⚠️ GAP |
| [Follower & Companion System](./06-game-systems/follower-companion-system.md)            | Familiars, followers, recruited monsters, field/tower roles, recovery            | ⚠️ GAP |
| [Tower Defense & Domain Events](./06-game-systems/tower-defense-domain-events.md)        | Forecast, preparation, assault, consequences, doctrine, floor stakes             | ⚠️ GAP |
| [Audio System](./06-game-systems/audio.md)                                               | Full SFX + music pipeline                                                        | ⚠️ GAP |
| [Save System](./06-game-systems/save-system.md)                                          | Slots, cloud, versioned saves                                                    | ⚠️ GAP |

---

### [07 — Testing](./07-testing/README.md)

Lock everything in with tests. **Start after each section above is feature-complete.**

| File                                             | Topic                                  | Status |
| ------------------------------------------------ | -------------------------------------- | ------ |
| [Unit Tests](./07-testing/unit-tests.md)         | Phase F1: vitest coverage targets      | 🚧     |
| [E2E Playwright](./07-testing/e2e-playwright.md) | Phase F3: full flow automation         | 🚧     |
| [Performance](./07-testing/performance.md)       | Phase G1 + F4: 60fps targets, budgets  | 🔲     |
| [Bot Testing](./07-testing/bot-testing.md)       | GameBot B1-B6 done, NS8 + CI remaining | ✅/🔲  |

---

### [08 — Polish & Release](./08-polish-release/README.md)

Last pass. **Nothing here starts until 07 is green.**

| File                                                  | Topic                                       | Status |
| ----------------------------------------------------- | ------------------------------------------- | ------ |
| [UI/UX](./08-polish-release/ui-ux.md)                 | Phase G2: HUD, menus, feedback              | 🚧     |
| [Game Feel](./08-polish-release/game-feel.md)         | Phase G3: screen shake, hit stop, polish    | 🚧     |
| [Accessibility](./08-polish-release/accessibility.md) | Phase G4: gamepad, colour-blind, text scale | 🚧     |
| [Documentation](./08-polish-release/documentation.md) | Phase H1: update all .md docs               | 🔲     |
| [Fundraising](./08-polish-release/fundraising.md)     | Phase H2: campaign + itch.io/Steam          | 🔲     |
| [Milestones](./08-polish-release/milestones.md)       | M1–M5 delivery gates                        | 🔲     |

---

## ⚠️ Identified Gaps (missing from any existing TODO)

| #   | Gap                                                                                                                                                                 | Impact        | Where to fix                                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ---------------------------------------------------------- |
| G1  | **Game World Integration** — realm terrain/river/chunking (RI-1 to RI-4), settlement spawning/roads/population/boundary/LOD (SI-1 to SI-6), dungeon entrance prop + site metadata (DI-1, DI-2, DI-2b), and cave/glade entrance props + realm placement (CG-1, CG-2, CG-3) data-transform/mesh/geometry slices all shipped as pure/tested modules; cave/glade entrances are now also fully wired into the live `OverworldScene.ts` (props render, discovery prompt + `[E]` toast, minimap icons, save persistence), the live settlement pipeline (already independently building/roads/NPCs) gained SI-4 boundary-crossing toasts, and the live dungeon pipeline (already working) had its exit-position bug fixed (DI-3) and site-family/reward-bias metadata wired live onto `DungeonEntry` + surfaced in the discovery toast (DI-2b) — **⚠️ audit (2026-08) found Studio and live game run separate, unrelated generators for realm/terrain and settlements (see [STUDIO-LIVE-PARITY.md](./02-game-world-integration/STUDIO-LIVE-PARITY.md)); quest/loot/elite-recruit consumption of that metadata (DI-4b), cave/glade floor scene transition (CG-4), and settlement LOD (SI-5) are now blocked-by-design pending the parity work's P0/P1/P3, not just "still needed"** | Blocking demo | `02-game-world-integration/`                               |
| G2  | **Asset Library UI** — partial; save/preview/export/import/rename/duplicate/delete exist for Studio asset types, but full creator/runtime coverage is still missing | Medium        | `01-overworld-studio/asset-library.md`                     |
| G3  | **Game Inventory** — stabilize the master contract of procedural asset outputs so phase-01 tools have a clear source of truth                                       | Medium        | `01-overworld-studio/game-inventory.md`                    |
| G5  | **Audio System** — referenced everywhere but has no phased plan                                                                                                     | High          | `06-game-systems/audio.md`                                 |
| G6  | **Save System** — basic save exists, no slots/versioning/cloud plan                                                                                                 | Medium        | `06-game-systems/save-system.md`                           |

| G8  | **Day/Night integration** — DayNightSystem exists but NPC schedules, weather, world events not planned                                                              | Low           | `06-game-systems/`                                         |
| G9  | **Performance budgets** — G1 says "60fps" but no per-system budget (triangles, draw calls, memory)                                                                  | Medium        | `07-testing/performance.md`                                |
| G10 | **Creature Creator expansion** — CC-1 to CC-6 phases (sub-races, clothing, body morphing)                                                                           | Medium        | `04-characters/creature-creator.md`                        |
| G11 | **Tomes / Research / Spellcraft** — book progression, library state, research bridge, spell mutation lacked a dedicated plan                                        | High          | `06-game-systems/tomes-research-spellcraft.md`             |
| G12 | **Alchemy / Transmutation / Crafting** — core material-conversion pillar lacked a phased production plan                                                            | High          | `06-game-systems/alchemy-transmutation-crafting.md`        |
| G13 | **Follower & Companion System** — quest rewards and recruits lacked a dedicated runtime/roster/assignment plan                                                      | High          | `06-game-systems/follower-companion-system.md`             |
| G14 | **Tower Defense / Domain Events** — companion-centered defense and tower pressure loop lacked a dedicated plan                                                      | High          | `06-game-systems/tower-defense-domain-events.md`           |
| G15 | **05 ↔ 06 circular dependency + false-done claims** — quests.md/story-arcs.md reference follower/alchemy/defense reward & event types that TRS/ATC/FCS/TDE (all zero-code, design-only) would need to deliver; also two claimed-done items don't exist in code: `customPrincess` toggle (princess-creator.md) and `survive_wave` beat type (story-arcs.md — silently auto-completes via a placeholder, `WaveManager` doesn't exist) | High          | `05-content/`, `06-game-systems/`                          |
| G16 | **Organic World Tiles** — terrain/shorelines, building corners, and settlement plot layout are all still fundamentally square-grid-typed (per-cell/per-tile, neighbour-smoothing bolted on after the fact — the just-shipped shoreline noise-wobble is the latest example). Research into Townscaper's dual-grid marching-squares + jittered-triangle relaxed-mesh technique is complete; a phased roadmap (Phase 0 shared case-table infra ✅ shipped, Phase 1 real dual-grid shorelines ✅ shipped, Phase 2 building corners ✅ chamfer classification shipped (kit-of-parts mesh-swap deferred, needs user check-in) **+ 2026-09-02 follow-up: BlockKit chamfer generalized to a true rounded arc, and rounded corner posts added to the human/generic default builder (previously zero corner treatment) — see `organic_world_tiles_todo.md`'s Phase 2 follow-up note**, Phase 3 organic settlement plots ✅ relaxed-mesh utility shipped (no live settlement integration yet, needs open questions resolved with the user) **+ 2026-09-02 investigation: found the settlement generator already uses independent organic techniques (jittered-Voronoi wards, an existing `fillWardOrganically()` layout, Chaikin-smoothed roads) — deferred again, now because it's no longer clearly missing rather than too risky, see roadmap doc's Phase 3 follow-up note**, Phase 4 dungeons ✅ wall-corner pilaster fix shipped (narrow scope; procedural dungeon generation deliberately not attempted), Phase 5 props ✅ lattice-deform utility shipped (no live prop integration yet)) is written; **all 6 phases (0-5) have shipped at least their core scope** — see `organic_world_tiles_todo.md`'s own status line for what's deferred and why. **+ 2026-09-02: Phase 6 added** — procedural race-by-race building construction, POC'd on an elven stone-tower kit (octagon ring-stack, real per-course block geometry measured against a cheap textured alternative, hybrid stone+living-tree decoration), wired into elven's watchtower/tower kinds. Found via live verification that watchtower/tower aren't currently reachable through the live settlement generator at all (no `WARD_TO_KIND` entry for either) — verified instead via the `showroom.html` dev tool. **+ 2026-09-02 follow-up: added a Settlement Lab "kind override" dev tool** (`SettlementLabPanel.ts`/`SettlementLabScene.ts`/`BuildingTypeMap.createSettlementBuildingDna`'s new optional `buildingKind` param/`SettlementRenderer`'s `forceBuildingKind`) so a settlement's buildings can all be forced to one chosen kind (e.g. faction `elven` + kind override `watchtower`) via Overworld Studio's Settlement tab → "Play in 3D" — this is what the user asked for to visually isolate/test the tower, and is meant to be reused for every future race/building-kind in this rollout; it does NOT make watchtower naturally spawn in a normal (non-overridden) settlement, that reachability gap is still open. **+ 2026-09-02 follow-up (2 rounds)**: (a) fixed a real bug where the Settlement Lab panel had zero CSS and rendered invisible below the fullscreen canvas (now `position:fixed`, verified visible/on-top live); replaced the "kind override" dropdown with an automatic per-faction map (`POC_KIND_OVERRIDE_BY_FACTION` in `SettlementLabScene.ts`) so picking faction=elven alone shows only watchtowers, no extra UI. (b) User feedback after seeing it live — "the only variation is the top and the height" — triggered a dedicated research pass (Townscaper technique, Godot addons, Three.js libraries) that found this repo's own `RelaxedMeshGrid.ts` (the exact Townscaper jitter+relax technique) was built but never wired to buildings; shipped 4 additive shape-variety techniques (per-vertex/per-floor coherent jitter, footprint drift/rotation, 4 named silhouette profiles tapering/tiered/leaning/waisted, all feeding existing mesh code) via new `StoneTowerSilhouette.ts` — live-verified via Playwright screenshots showing genuinely different tower silhouettes side-by-side (leaning/tapering/waisted), not a uniformly-scaled repeat. **+ 2026-09-03 follow-up (6.4d)**: added kit-of-parts feature variety — 3 window types (pointed-arch/oculus/cross-mullion) x 3 sizes re-rolled per floor, 2 entrance archway styles always present at the base, a seeded ~40% top-floor projecting balcony (corbels+deck+parapet), and an extended wall-prop catalog (vine/moss-patch/banner/none, up from vine-or-nothing). **+ follow-up (6.4e)**: user screenshot feedback flagged the windows/entrance as flat "basic geometry" clashing with the wall's real block-course depth, then 5 reference tabletop-tower-kit images flagged overall variety as still just parametric tweaks on one shape, not genuinely distinct kit assemblies — shipped a full technique rebuild (shared recessed-carved-opening geometry for windows/entrance, corner quoins, a genuinely open rib-post balcony instead of a solid drum, a relief-detailed classic roof, and a new structurally-distinct pagoda roof archetype), including a live-verification-driven second pass on the pagoda archetype after screenshots showed its first version's two tiers visually blending into one ambiguous cone (fixed by truncating the lower tier so only the upper tier has a point) — see `organic_world_tiles_todo.md`'s  **+ 2026-09-03 follow-up (6.6)**: moved to the SECOND elven building type (the living-tree home used by house/terraced/villa/inn/blacksmith -- the highest-frequency elven building type), same research -> learn -> plan -> build -> test cycle under user-enabled autopilot. Research surfaced The Elder Scrolls' Valenwood/Falinesti "moss shared roof" motif and real treehouse ring-beam/knee-brace engineering as directly-usable design rules. Shipped ring-beam+knee-brace bands per floor (was one, fixed at the neck), carved window openings (`ElvenTrunkWindows.ts`, extending the door's proven occupancy-carving technique), 2 entrance styles, and 2 canopy archetypes -- and, during live-verification prep, found and fixed a real bug where the 2 new picker functions were fully implemented/tested in isolation but never actually called from the live builder, so every building silently defaulted to the same style regardless of seed. Settlement Lab's POC override switched from watchtower to house. See `organic_world_tiles_todo.md`'s Phase 6 (6.4b-6.6c) for full detail. **+ 2026-09-03 follow-up (6.6c)**: third elven building type -- the market stall ("shop" kind). Research confirmed real market stalls are 1-2 walls (not 4) and found Zelda BOTW's Great Deku Tree literally nests shops into a living tree (validating the existing sapling-platform concept). Added a facesOverride option to buildWallSurfaceBlocks() for genuinely partial (not full-ring) walls, then built ElvenMarketStallKit.ts reusing the tower kit's carved-opening technique for a counter cutout, a striped fabric awning, a small living sapling canopy, and a hanging trade-sign. Old buildElvenShop/addBlockElvenTrunk deleted as dead code. A real placement bug (counter/awning/sign facing the wrong direction) was caught via a live screenshot and fixed. **+ 2026-09-03 follow-up (6.6b)**: user feedback -- "we dont go with that old block design... more in style with the tower for building the structure" -- rebuilt the living-tree home on the stone tower kit's own real block-course + carved-opening construction technique instead of BlockKit, extracting a shared buildTowerKitCore() (base+rings+roof) so both building families reuse the exact same proven pieces (all 30 existing tower tests pass unchanged, proving the extraction is behavior-preserving). New ElvenTreehouseKit.ts forces a living-canopy roof always and respects dna.floors exactly. Old buildElvenVilla/addPlankRing/addRingBraces deleted as dead code; buildElvenShop intentionally untouched (deferred to its own future round). **+ 2026-09-04 follow-up (6.6d)**: user feedback on the shipped treehouse home -- visible seethrough gaps at the trunk/roof seam ("no ground there"), and every building forced the same living-canopy roof ("the roofs can be more like the tower design roofs"). Root cause of the gap: StoneTowerWallSurface.ts's per-course block walls are a hollow shell with no floor anywhere; new StoneTowerFloorCap.ts's buildFloorCap() (a filled octagon disc) now caps every buildTowerBase()/buildTowerWallRing() group, closing every seam for both the tower and treehouse families. StoneTowerRoofCap.ts's pickRoofArchetype()/buildTowerRoofCap() gained an optional weight-table parameter (default unchanged for the tower); a new RESIDENTIAL_ROOF_ARCHETYPE_WEIGHTS (living 45%/classic 30%/pagoda 25%) gives the treehouse family real roof variety while keeping living the plurality choice. A real regression was found and fixed during live verification, not by any unit test: the floor cap's custom geometry initially had no uv attribute, which silently broke THREE's mergeGeometries() when SettlementRenderer.ts's whole-building mergeGroupMeshesByMaterial() pass tried to merge it alongside uv-having wall blocks -- and a failed merge drops AND disposes the WHOLE material bucket, not just the offending mesh, so affected buildings lost their entire wall geometry and rendered as flat black silhouettes in the live settlement generator. Fixed by adding a planar uv attribute; added a dedicated regression test reproducing the exact failure. Live-reverified via Playwright across 3 seeds: full detailed geometry everywhere, with visibly distinct classic/pagoda/living roofs co-existing in one settlement. **+ 2026-09-04 follow-up (6.6e)**: fourth elven building type -- the chapel, replacing the old standing-tree-stones "Ancient Shrine" with a Gothic-elven nave + apse + bellcote + forecourt. Core tension: chapel's fixed 4x8 footprint is genuinely elongated (1:2), unlike every prior tower-kit building's roughly square/circular shape built around a single-radius octagon. Research found a 4x8 rectangle is already architecturally correct, a small octagonal apse at the altar end is genuine real-world architecture (no compromise needed), small parish churches use a cheap bellcote instead of a full second tower, and round-tower churches are a named precedent for docking a round piece flush against a flat wall. Key finding: buildWallSurfaceBlocks()'s existing facesOverride has no dependency on regular-octagon faces, so it builds a real rectangular nave with zero changes to that function. Added rectanglePoints()/rectangleFaces()/facePointAt() (StoneTowerShape.ts), a pointsOverride generalization to buildFloorCap()/buildQuoins(), a new buildGableRoofCap() (StoneTowerGableRoof.ts, the one genuinely new roof primitive), and ElvenChapelKit.ts composing the nave, an apse (existing unmodified radial machinery, living-canopy roof, relocated sacred crystal), a bellcote, and a relocated standing-stone forecourt. Old buildElvenChapel deleted; docs/BUILDINGS.md's stale elven chapel row synced. A real bellcote placement bug (positioned inside the wall/roof volume, fully occluded) was found and fixed via live verification, not any unit test. **+ 2026-09-04 follow-up (6.6f)**: with 4 elven kits shipped, generalized the Settlement Lab's single-kind-forcing override into a per-building SHOWCASE (SettlementRenderContext.forceBuildingKind now also accepts a (building, index) => BuildingKind | undefined callback) so faction=elven now shows all 4 kit families together in one 'Play in 3D' session instead of forcing every building to one kind -- the callback forces only the settlement's first building to 'watchtower' (the one elven kind with no WARD_TO_KIND entry, otherwise unreachable), leaving every other building on its natural ward-based variety. Live-verified via Playwright against a dev server running from the actual worktree (a stale server from the separate main checkout does not pick up worktree-only changes). **+ 2026-09-04 (6.7) ALL-RACE BUILDING PROGRAMME — PLANNING ONLY, awaiting user approval**: after reviewing the 4 elven kits together, the user rejected 2 of them outright ("The church and stall will need to be not just improved but made from scratch again") and scoped up to every building type for every race, driven by newly-supplied reference art, with "ABSOLUTELY NO BACK GEOMETRY BS! NO BLOBS OR SQUARES FOR WINDOWS OR SUCH!" — explicitly asking for ALL research/design/planning first, one branch per race, then a review stop before any implementation. Root cause of both rejections traced to code: round 6.6b upgraded the WALLS to block-course masonry (which the user praised) but left roofs, openings and caps on the old primitives — buildGableRoofCap is two flat planes (the chapel's black roof), buildLivingRoofCap is a BlockKit voxel blob (the apse's white cubes, i.e. the very technique 6.6b replaced for walls), buildRecessedArchOpening gives a proud frame + a bare dark cavity with no sill/mullion/glazing (the "small gray round and rectangular things"), and buildArchShape is a stylised two-straight-line point rather than a true two-centred gothic arc. Delivered (docs only, zero source changes, tsc still exactly 144 baseline): a binding cross-race DOCTRINE (docs/superpowers/specs/2026-09-04-modular-building-kit-doctrine.md — seven non-negotiable rules headed by the DEPTH LADDER (nothing coplanar, +0.30 buttress down to -0.20 glazing, because openings read from shading discontinuity not albedo) and the FIVE-PIECE OPENING MINIMUM (recess, proud surround, sill, mullion, set-back opaque glazing — pieces 3/4/5 are the entire gap behind the rejection), plus a KEEP/REBUILD/RETIRE/MISSING audit, a 3-tier shared kit under src/world/buildings/kit/, the canonical 8-kind roster, and decisions D1-D8); a 1485-line external RESEARCH REPORT (docs/superpowers/research/2026-09-04-modular-building-techniques-research.md — kit-of-parts/socket model, facade split grammars, true two-centred gothic arch construction, voussoirs/jambs/sills/hood moulds/foils/mullions, shingle gauge lattices, interlace knotwork, columns/arcades, the seven ruin signals, three.js r170 specifics incl. the CSG verdict, anti-patterns, and R1-R16); and 9 DESIGN SPECS + 9 IMPLEMENTATION PLANS, one race per pushed branch race/<race>-buildings. Elven carries the shared kit (18 of its 41 tasks are [SHARED KIT]) and both from-scratch rebuilds: the chapel as a brick-built gothic church RUIN (seeded per-course occupancy erosion so the break line reads as individual bricks — which is why CSG is rejected, a boolean cut gives exactly the smooth planar face the user hates) and the market stall as an elven GAZEBO/PAVILION (stepped plinth, columns, lattice/vine canopy). Also committed concept_art/reference/buildings/ itself, which was UNTRACKED — the design source for the whole programme existed only in the local main checkout's working directory and was in no commit. Cross-race findings (doctrine Part 9): watchtower/tower are STILL unreachable in real settlements (6.6f only worked around it in the lab) — proposed one shared fix, map the ANCHOR building of the gateward ward to watchtower (gateward is the gate district, is already in MAIN_ROAD_WARD_TYPES so it sits on a main road at the settlement edge where a tower reads best, farm still covers house, and mapPlacedBuildingToDna already has isAnchor — but buildingHalfExtents must apply the same rule or spacing breaks); several races currently alias house/terraced/inn/blacksmith all onto one villa builder; and slime has NO reference art, so its spec presents three alternative directions with a recommendation and is explicitly blocked on a user decision. **+ 2026-09-05 follow-up (6.8, slime)**: user resolved the slime art-direction gap with a new brief -- a Pokemon-Ditto-esque mimic/absorption culture that observes and reproduces another race's building shell, then re-skins it in a rotating neon hue family (mint/azure/bubblegum-pink/violet/cyan, never one fixed green) with a rounded gel-mimic silhouette and mandatory drip points. Implemented as host-shell -> light Ruinate damage -> slime accretion overlay, reusing the shared Tier 1-3 kit already merged to main (lattice-dome canopy, interlace ornament, ruinate course erosion/debris, lathe columns) -- slime is the first race to build on that shared kit end-to-end. All 8 canonical kinds shipped (house/terraced/shop/inn/blacksmith/villa/chapel/watchtower), wired into FACTION_BUILDING_VARIANTS['slime'], old translucent Sphere/Cylinder blob primitives deleted as dead code. Settlement Lab's POC_KIND_OVERRIDE_BY_FACTION gained a slime entry so all 8 kinds review together in one settlement. Real bug found+fixed during verification (same class first caught in 6.6d for StoneTowerFloorCap.ts): two slime geometry sources (createSaggingMembraneGeometry in SlimeAccretionKit.ts, createAngularRubbleGeometry in the shared Ruinate.ts) built custom BufferGeometry with no uv attribute while sharing a material with uv-having sibling meshes -- mergeGeometries() fails silently on that mismatch and mergeGroupMeshesByMaterial() (MeshMergeUtils.ts) disposes the ENTIRE material bucket regardless of merge success, so every one of the 8 slime kinds was silently dropping real geometry in production (89 console warnings pinpointed it). Fixed by adding a uv attribute to both (both materials are flat-colour with no texture map, so no visual-precision requirement). Live-verified via Playwright: faction=slime showcase renders all 8 kinds with zero console errors, visible neon-hued roofs with organic rounded accretion and gel-drip pillars, no holes/missing geometry across 2 seeds/zoom levels. Full regression clean (13 pre-existing baseline failures unrelated to slime files, 1 confirmed-non-flaky parallel-run timeout); tsc holds at the 146-error baseline. **+ 2026-09-05 follow-up (6.9, dwarven)**: seventh race, second non-elven — unlike slime, dwarven had rich reference art from the start (6 images), so no art-direction brief was needed: compressed, weighty stonecraft (rock plinths, stepped/battered walls, small deep-set openings, corbelled chimneys, angular chevron ornament, metal banding, visible industry). Reused the shared Tier 1-3 kit and added 6 new dwarven-flavoured shared-kit modules (RockPlinthSkirt/SteppedBatterProfile/CorbelledChimneyStack/AngularOrnament/MetalBanding/PipeworkVent) plus a MassComposer helper for multi-mass villas/inns. All 8 canonical kinds shipped in DwarvenBuildingKit.ts, wired into FACTION_BUILDING_VARIANTS['dwarven'], replacing the old stepped-tier BlockKit hall (deleted as dead code). Settlement Lab's POC_KIND_OVERRIDE_BY_FACTION gained a dwarven entry. No new uv-attribute merge-drop bug this time (static sweep confirmed every custom BufferGeometry in the shared kit + dwarven's own files sets a matching uv attribute); one test-authoring bug fixed (a buttress-count assertion matched substrings instead of exact top-level names) and one timeout tuning fix (dwarven's heavier masonry needed the Settlement Lab showcase test's timeout raised to 15s, mirroring existing precedent). Live-verified via Playwright across 2 seeds/zoom levels: real pitched roofs, recessed openings with proud surrounds, rock-plinth bases, corner buttresses, multi-mass silhouettes, zero console errors, no holes/back-geometry/floating pieces. Full regression: 91+131+13 dwarven-adjacent tests green; full suite's 14 failures are the same 9 pre-existing baseline plus 5 confirmed non-flaky-in-isolation parallel-load timeouts (plus 3 more OverworldScene suite-timeout flakes), none a real regression; tsc holds at 146. Remaining 5 races (vampire/undead/vulperia/fae/human) still carry only their design spec + implementation plan from 6.7 -- no implementation commits yet. **+ 2026-09-06 follow-up (6.10, orcish)**: eighth race, third non-elven — reference art (concept_art/reference/buildings/orc/) plus the 6.7 design spec both pointed to a distinct "heavy/rough" silhouette from dwarven's dressed stonecraft: lashed round-timber framing, stretched-hide/leather roof panels and wall infill, raw round/ellipse openings framed by exposed timber, and tribal salvage ornament (banners, hung shields, bone trophies). Added 4 new orcish-flavoured shared-kit modules (LashedTimber/HidePanel/RibbedRoof/SalvageSpoils) plus orcish-specific OrcishMaterials/OrcishOpenings. All 8 canonical kinds shipped in OrcishBuildingKit.ts (watchtower last: tapered-log legs on a stance axis, 2 tiers of X cross-bracing, a rung ladder, a boxed plank platform with a framed ladder-hatch, a parapet-rail axis with 3-4 lookout slits, a roof-cap axis, and a signal-prop axis), wired into FACTION_BUILDING_VARIANTS['orcish'], replacing the old lashed-hut BlockKit grid (deleted as dead code; the grid generator itself kept in FactionBlockProfiles.ts per the dwarven precedent). Settlement Lab's POC_KIND_OVERRIDE_BY_FACTION gained an orcish entry. No new uv-attribute merge-drop bug (a targeted sweep found no raw hand-rolled BufferGeometry in the new orcish code), but a related named-child-loss footgun was found and fixed: mergeGroupMeshesByMaterial() on the watchtower's plank-platform sub-group was collapsing named hatch-lip-* meshes the tests looked up by name, fixed by not merging that sub-group. Live-verified via Playwright across 5 seeds and multiple camera heights: real proud/recessed round and arched openings with visible depth, lashed round-timber framing, staggered hide-panel roofs with ribbing, corbelled-stone forge chimneys with glowing arch hearths, and the forced watchtower's tapered legs/X-bracing/plank platform/conical roof with lit lookout slits — zero new console errors, no holes/back-geometry/floating pieces. Full regression: orcish + shared-kit + registry + Settlement Lab suites 528/530 green, the only 2 failures being the same pre-existing parallel-load-timeout flakes already documented for slime (6.8)/dwarven (6.9); tsc holds at the 146-error baseline. **+ 2026-09-06 follow-up (6.11, vampire)**: eighth race, fourth non-elven — the 6.7 spec called for vertical/aristocratic/MAINTAINED Gothic-Revival/Second-Empire manors, explicitly not "undead but purple" (closed shuttered windows not broken ones, intact steep roofs not missing walls). Reference art's one GIF still could not be viewed by this session either, so proceeded per the spec's documented JPG+research fallback. Added 2 new shared-kit modules (`Shutter.ts` louvred/panelled closed shutters, `OrielBay.ts` corbel-supported projecting bay window) plus one new `RoofMassing.ts` archetype (`buildMansardRoof()`); every other kit module the plan cited already existed from dwarven/orcish. All 8 canonical kinds shipped in `VampireBuildingKit.ts` (chapel built as an inhabited private family chapel, not a mausoleum, to preserve the "not funerary" distinction), wired into `FACTION_BUILDING_VARIANTS['vampire']`, replacing the old BlockKit spire/villa/chapel/shop functions entirely. Settlement Lab's `POC_KIND_OVERRIDE_BY_FACTION` gained a vampire entry. Found + fixed a real z-extent bbox-overflow bug in the terraced/shop builders (an inappropriately small `blocksPerFace: 2` override caused running-bond masonry corner overhang past the nominal wall boundary — caught by this race's own TDD test, fixed by removing the override so the function's own face-length-scaled default applies); no new uv-attribute merge-drop instance found in the 2 new shared-kit modules. Live-verified via Playwright across 3 seeds: dark vertical manor silhouettes, steep mansard/pyramidal roofs with visible tile texture, coursed masonry with corner quoins, closed shuttered openings reading as recessed with proud surrounds, zero new console errors. Full regression: vampire (63) + FactionBuildingVariants (147) suites green; whole-suite 14 failed/3907 passed/4 skipped confirmed via git-stash comparison to be the exact same pre-existing baseline failures as slime/dwarven/orcish, none touching vampire code; tsc holds at the 146-error baseline. **+ 2026-09-06 follow-up (6.12, undead)**: ninth race, fifth non-elven — the doctrine's Part 9 addendum explicitly distinguishes undead from the just-shipped vampire: communal/horizontal/decaying/funerary (mausolea, ossuaries, monuments, crypt walls, classical friezes, spolia) versus vampire's private/vertical/maintained/aristocratic, with damage that EXPOSES age rather than concealing it behind closed shutters. Added 5 new shared-kit modules (`Frieze.ts`, `Pediment.ts`, `Railing.ts`, `LanternKit.ts`, `MonumentKit.ts`) plus undead-specific `UndeadNecropolisPalette.ts`/`UndeadOpenings.ts` (tomb/arcade five-piece openings, some deliberately broken but still fully backed by real recess/surround/sill geometry per the doctrine's no-back-geometry rule)/`UndeadFacadeModules.ts`/`UndeadLotDressing.ts` (grave markers, iron rails, rubble). Reused `Ruinate.ts` (seeded per-course erosion/debris, built for elven's chapel ruin) and `SalvageSpoils.ts` (orcish's spolia primitive) as-is — both were direct fits for undead's ruin aesthetic with zero new shared-kit erosion code needed. All 8 canonical kinds shipped in `UndeadNecropolisKit.ts` (chapel as a ruined shrine, watchtower as a crumbling stepped-battered tower with a broken parapet, not an intact cap), wired into `FACTION_BUILDING_VARIANTS['undead']`, replacing the old BlockKit lich-tower/ossuary-spire functions (~192 lines) entirely. Settlement Lab's `POC_KIND_OVERRIDE_BY_FACTION` gained an undead entry. No new uv-attribute merge-drop instance found (a targeted sweep confirmed zero hand-rolled `BufferGeometry`/`setAttribute` calls anywhere in the new undead files, despite heavy reliance on `Ruinate.ts`'s debris geometry). Live-verified via Playwright across 3 seeds (42/12345/918273) with digitally-cropped close-ups: real recessed arcade-style doorways with visible depth (not bare holes), grounded rubble/debris and small mausoleum props (never floating), stepped-plinth courses with corner buttresses, iron-railed graveyard lot dressing with pale headstones, and a distinctly ruined/broken watchtower top (jagged remnant railings, stepped battering) reading clearly distinct from vampire's neat conical mansard cap — zero new console errors across all seeds (only a benign GPU-driver perf warning and a pre-existing player-character `PrincessDefaults` warning unrelated to buildings). Full regression: undead + shared-kit suites (374 tests) green, `FactionBuildingVariants.test.ts` (222 combined with `FactionBlockProfiles.test.ts`) green, `SettlementLabScene.test.ts` (15) green; whole-suite runs (twice) showed 15-20 failures out of ~4034 tests, a non-deterministic overlapping-but-different set across the two runs matching the same pre-existing environment-flake pattern documented for slime/dwarven/orcish/vampire, none touching undead files; tsc holds at the 146-error baseline. Remaining 2 races (fae/human) still carry only their design spec + implementation plan from 6.7 — no implementation commits yet. **+ 2026-09-07 follow-up (6.13, vulperia)**: tenth race, sixth non-elven — fox-folk WARREN architecture: warm, watchful, clever buildings tucked into grassland/savanna earth, coursed cob/timber walls, many small framed openings, and thick sod/turf roofs that sweep down like sheltering hills without ever becoming smooth green blobs. Root-caused and fixed a real cross-race shared-kit bug while building the new `TurfRoof.ts` module: `computeSlope()`'s outward-normal correction negated `outDir` alone instead of also flipping `spanDir`, silently breaking the right-handed orthonormal basis fed into `makeBasis()` for any slope with `sign=+1` — corrupting the world-space rotation of one of every gable's two fascia meshes (this affects any future race's use of `TurfRoof.ts`, not just vulperia's own calls); fixed by flipping both vectors together, which is mathematically guaranteed to preserve `outDir = cross(spanDir, climbDir)`. Added `TurfRoof.ts` (segmented turf-roof planes: rafters/board-deck/board-ends/turf-stop/soil-edge/grass-top layers per slope, named dormers, porch cutouts — the first genuine course/tile-based turf ROOF surfacing in the shared kit, generalized from `ShingleSurface.ts`'s tile-course precedent) plus vulperia-specific `VulperiaPalette.ts`/`VulperiaGrounding.ts` (earth berm + grass-berm cap + front steps — the doctrine's rigorous berm/skirt at every terrain contact, following dwarven's `RockPlinthSkirt.ts` precedent)/`VulperiaOpenings.ts` (round-watch/eyebrow-dormer/shuttered-window presets over the shared five-piece `OpeningParts.ts` primitives, reusing vampire's `Shutter.ts` directly for its "closed, not broken" framed-window language)/`VulperiaLotDressing.ts` (planter barrels, twig den markers, crate stacks, low fences). All 8 canonical kinds shipped in `VulperiaBuildingKit.ts` (villa as a multi-mass cross-gabled "Wanderer's Den" with lantern posts, shop as an awninged "Night Market" stall, blacksmith as a "Tinkerer's Shop", chapel/house/watchtower all bespoke), wired into `FACTION_BUILDING_VARIANTS['vulperia']`, replacing the old BlockKit earthen "den mound" functions (~300 lines: `vulperiaMound()`/`addBlockDenMound()`/`addRoundDoor()`/`addChimneyStack()`/etc.) entirely — `buildVulperiaDenMoundGrid()` in `FactionBlockProfiles.ts` explicitly preserved since `FactionTerritoryProps.ts` still uses it independently for lore/ground-dressing den mounds, unrelated to the building-kind system. Settlement Lab's `POC_KIND_OVERRIDE_BY_FACTION` gained a vulperia entry. No new uv-attribute merge-drop instance found (a targeted sweep of every custom `BufferGeometry`/`setAttribute` call across the new vulperia files and `TurfRoof.ts` found all set a matching uv attribute; a regression test was added to `TurfRoof.test.ts` covering the handedness fix). Live-verified via Playwright against a dev server started from this worktree on an unused port across 3 seeds: real segmented turf roofs with visible ridge/hip seams, dark verge trim, and chimney stacks (never a smooth dome), small recessed framed window openings, lantern-lit warm night-time massing, the legacy earthen den-mound preserved separately as ground-dressing lore (not part of the building kit), and zero new console errors (only the same benign GPU-driver perf warning and pre-existing `PrincessDefaults` warning seen in every prior race's verification). Full regression: vulperia + shared-kit `TurfRoof.test.ts` suites (63 tests) green, `FactionBuildingVariants.test.ts` (167 tests) green, `SettlementLabScene.test.ts` (16 tests) green; whole-suite run showed 15 failures out of 4109 tests, all pre-existing/unrelated to any vulperia file (confirmed deterministic in isolation for 13 of them, and the remaining 2 — both `SettlementLabScene.test.ts` timeouts — confirmed passing 16/16 in isolation, i.e. full-suite parallel-load flakiness, not a regression); tsc holds at the 146-error baseline. Remaining 2 races (fae/human) still carry only their design spec + implementation plan from 6.7 — no implementation commits yet. **+ 2026-09-06 follow-up (6.14, fae)**: eleventh race, seventh non-elven — TINY INHABITED FAIRY ARCHITECTURE per the 6.7 design spec and its own reference art: stump/fungal cottages, petal-roof shops, curled shingle-tower spires, root-flare foundations, and oversized warmly-glowing doors/windows, assembled as legible kit-of-parts (never free-form sculpted blobs) then optionally deformed as a bounded assembly. Built two new shared-kit primitives fae's two signature roof asks needed and no existing module covered: `RadialMushroomCap.ts` (radial rib/gill dome-cap construction, reusable by any future race wanting a real toadstool-cap roof) and `ShingleSurface.ts`'s new `buildCurledConeShingleRoof()` (a curved-directrix tile-course roof so courses spiral/curl toward the apex while every tile butt and eave edge stays real, separately-shaded geometry — never a smooth cone). Also built `AssemblyLatticeDeform.ts`, a bounded seeded lattice-deformation utility (lean/bulge/twist/taper axes on a control lattice, not per-vertex noise) applied to each building's hall+openings+roof sub-assembly, deliberately excluding grounding/lot-dressing so ground contact never desyncs. Added `FaePalette.ts`/`FaeOpenings.ts` (oversized 30-45%-facade-width round-arch/petal-lancet/oculus five-piece openings)/`FaeGrounding.ts` (root-flare foundations, following dwarven's `RockPlinthSkirt.ts` precedent)/`FaeLotDressing.ts` (toadstool clusters, glowing fairy-ring markers). All 8 canonical kinds shipped in `FaeBuildingKit.ts` (~1230 lines), wired into `FACTION_BUILDING_VARIANTS['fae']`, replacing the old BlockKit toadstool-stalk-grid functions (villa/chapel/shop only) entirely as dead code — which also exposed and removed two now-fully-unused generic helpers (`mat()`/`addMesh()`) in `FactionBuildingVariants.ts` that had no callers left except fae's old code. Settlement Lab's `POC_KIND_OVERRIDE_BY_FACTION` gained a fae entry. No new uv-attribute merge-drop instance found (targeted sweep of every custom geometry call across all new fae files + `RadialMushroomCap.ts` + the new curled-cone roof builder). Live-verified via Playwright across 3 seeds, using the `V` WoW-camera-mode key binding for real facade close-ups: real curled tile-course roofs with visible tile butts/eave thickness, scalloped radial mushroom-cap roofs with visible rib/gill segmentation, oversized five-piece glowing openings (proud mullion surrounds, dark recess, warm amber glow, never a flat dark box/circle), bark-plank wall coursing, dense toadstool/fairy-ring lot dressing, no floating/back-geometry, zero new console warnings/errors. Full regression: fae + shared-kit suites (6 files, 61 tests) green, `FactionBuildingVariants.test.ts` + `FactionBlockProfiles.test.ts` green (274 tests); whole-suite run showed 17 failures out of 4175 tests, reproduced identically across two independent full runs (deterministic, not flaky), zero overlap with any fae/registry file — the same pre-existing baseline pattern documented for every prior race; tsc holds at the 146-error baseline. All 8 non-human races now have full 8-kind bespoke kits shipped; only human remains, deprioritized per this phase's own scoping note. **+ 2026-09-12 follow-up (6.15, human) — NINTH AND FINAL RACE, PROGRAMME COMPLETE**: human is the deliberately-deprioritized BASELINE settlement kit — familiar medieval-European vernacular (timber frame, plaster infill, stone ground floors, jetties, clay tile/slate/thatch roofs) designed to read as "built by many hands over centuries" via internal variety (split grammar + module swapping + seeded jitter) rather than an exotic silhouette. Delivered the programme's two flagship `[SHARED KIT]` contributions, both explicitly built for reuse by any future race: `TimberFrame.ts` (a proud structural half-timber frame — posts/rails/braces/studs at +0.08 WU — with recessed infill panels at 0.00/-0.04 WU, real depth-ladder construction, never a flat printed-texture half-timber look) and `HumanJetty.ts` (upper floors projecting 0.28-0.45 WU past the lower wall on a bressummer beam with exposed joist ends and corbel/knee-brace supports, not a simple enlarged upper box). Also shipped `ThatchRoofSurface.ts` (stacked fibrous bands, thick rolled eaves, pegged ridge cap) alongside the existing `ShingleSurface.ts` for tile/slate coursing, plus human-specific `HumanBuildingMaterials.ts`/`HumanOpenings.ts` (five-piece recess/surround/sill/mullion/glazing openings)/`HumanBuildingProps.ts`. All 8 canonical kinds shipped in `HumanBuildingsKit.ts` (~2000 lines): house/terraced/villa/inn/shop/blacksmith (timber-frame + jetty variety across all six), chapel (stone nave with real buttresses, voussoir-arch windows, a hand-built timber bellcote — the doctrine's chapel canonical kind), and watchtower (a 4-tier battered stone taper reusing dwarven's `SteppedBatterProfile.ts`, arrow loops built by reusing `buildHumanWindow()` at extreme proportions rather than hand-rolled geometry, and an unconditional small timber lookout hoarding so every watchtower instance — even this all-stone kind — still carries a genuine TimberFrame member, satisfying the cross-kind regression sweep). Mandatory per-facade asymmetry (off-centre doors, one differing shutter/panel/chimney/dormer) implemented throughout. Wired into `FACTION_BUILDING_VARIANTS` for all three human sub-factions (`human_rural`/`human_town`/`human_noble` — confirmed human was never previously wired into this registry, a pure addition not a replace) and into Settlement Lab's `POC_KIND_OVERRIDE_BY_FACTION` (`human` entry, same one-forced-watchtower-slot pattern as all 8 other races, since watchtower has no `WARD_TO_KIND` entry). No new uv-attribute merge-drop bug instance found (targeted sweep of all new human geometry). Two real regressions were caught and fixed by the fresh full-suite run after wiring: `FactionBuildingVariants.test.ts`'s "returns null for an uncovered pair" test and `SettlementLabScene.test.ts`'s "other factions without a shipped override" test both previously used `human`/`human_town` as their canonical example of an *uncovered* faction — now that human is wired, both were updated to use `draconic` (a Faction value genuinely outside the 9-race programme) and an unrecognized faction string respectively, plus a new dedicated human showcase test was added mirroring every other race's. Live-verified via Playwright against a dev server started from this worktree (port 5199): all 9 factions (elven/slime/dwarven/orcish/vampire/undead/vulperia/fae/human) render real building counts (27-78 buildings per city-sized settlement) with zero console errors and each shows its own `POC override: showcase (all <faction> kits)` readout; human close-ups confirmed real proud timber cross-bracing over recessed infill panels, a rolled/bundled thatch roof, dark tile/slate roof coursing, and varied per-building silhouettes. Full regression: `tsc --noEmit` holds at the established 146-error baseline (zero human-related errors); whole-suite vitest run showed the same pre-existing/flaky timeout-driven failures documented for every prior race (main.startup.smoke/enemyLoader/towerGenerator/talentSystem/OverworldScene chunk-streaming/ResourceNodePlacer/WaterMaterial/WorldGenerator/StoneTowerKit/Tracery/VampireBuildingKit — reproduced as pure timeouts, not deterministic failures, when re-run in isolation), zero new failures attributable to human or registry files. **THIS COMPLETES THE FULL 9-RACE PROCEDURAL BUILDING PROGRAMME** (elven, slime, dwarven, orcish, vampire, undead, vulperia, fae, human) — every faction now has a full 8-canonical-kind bespoke kit-of-parts implementation (house/terraced/shop/inn/blacksmith/villa/chapel/watchtower), reachable both through normal settlement generation (7 kinds via `WARD_TO_KIND`) and Settlement Lab's showcase override (the 8th, watchtower), with zero leftover BlockKit/primitive-blob placeholder geometry for any race. **+ 2026-09-13 follow-up (6.16, human placement-bug fix, PR #60)**: a post-merge user visual review found human's props (lanterns, jetties, cornices, buttresses, signs, merlons, corbels, hoardings) reading as structurally implausible up close. Root cause: `placeOnFace()` set rotation plus an along-face offset relative to the face's own midpoint but never added that midpoint's absolute world position back in, so every depth-agnostic prop across all 8 human kinds landed pinned near the building's own centerline instead of flush against its wall -- a coordinate-math bug present since 6.15's original implementation, affecting already-shipped villa/chapel/watchtower geometry too, missed because Settlement Lab never reset its background/fog off dark interior-scene defaults (also fixed here). Fix: new `placeOnFaceAtDepth()` helper adds the face's absolute midpoint plus a small depth delta; ~30 call sites corrected. New regression test (`HumanFacePlacementRegression.test.ts`, 8 cases) asserts wall-flush minimum radial distance per kind, all passing; `tsc --noEmit` holds at 146; live Playwright re-verification confirms real wall-flush buttresses/jetties/cross-bracing. Lesson: existential geometry assertions (finite/non-NaN) don't catch wrong *placement* -- prefer positional invariants when a helper's job is spatial correctness. | Medium — art direction, not blocking | `organic_world_tiles_todo.md` (TODO root, cross-cuts 02 + 03) |

---

## Quick Reference: File → Phase Mapping

| Legacy Phase                            | New Location                                           |
| --------------------------------------- | ------------------------------------------------------ |
| OW-A/B/C/F1-F3                          | **DONE** ✅                                            |
| OW-D, OW-E, OW-F4                       | `01-overworld-studio/`                                 |
| PROC-A thru PROC-E                      | `03-procedural-pipeline/`                              |
| PC (Princess Creator)                   | `04-characters/princess-creator.md`                    |
| B (Enemy)                               | `04-characters/enemy-system.md`                        |
| C (Quests)                              | `05-content/quests.md`                                 |
| D (Abilities)                           | `06-game-systems/abilities-talents.md`                 |
| D+ (Tomes / Research / Spellcraft)      | `06-game-systems/tomes-research-spellcraft.md`         |
| D+ (Alchemy / Transmutation / Crafting) | `06-game-systems/alchemy-transmutation-crafting.md`    |
| D+ (Followers / Companions)             | `06-game-systems/follower-companion-system.md`         |
| D+ (Tower Defense / Domain Events)      | `06-game-systems/tower-defense-domain-events.md`       |
| E (Story + Solmor)                      | `05-content/story-arcs.md` + `solmor-encounters.md`    |
| NS (New Species)                        | `04-characters/new-species-expansion.md`               |
| F (Testing)                             | `07-testing/`                                          |
| G (Polish)                              | `08-polish-release/`                                   |
| H (Docs + Fundraising)                  | `08-polish-release/`                                   |
| Phase 5 (Environment Art)               | `03-procedural-pipeline/environment-art-system.md`     |
| Creative Mode (C1-C10)                  | `03-procedural-pipeline/PROC-D-creative-mode.md`       |
| Bot Testing (B1-B6)                     | `07-testing/bot-testing.md`                            |
| Level Editor (L0-L6)                    | **DONE** ✅ — see `archive/ASSET_LEVEL_EDITOR_TODO.md` |
| Creature Creator (CC-1 to CC-6)         | `04-characters/creature-creator.md`                    |
| Docs organization                       | `docs/overview.md` (master docs index)                 |
| Organic World Tiles (dual-grid/Townscaper) | `organic_world_tiles_todo.md` (cross-cuts 02 + 03)   |

---

## Source Files

- `TODO.md` (root) — master source of truth, guiding principles, art system decisions
- `TODO/archive/` — superseded files kept for reference only
